name: Deploy to Production
on:
  push:
    branches: [main]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Deploy to Vercel
        uses: amondnet/vercel-action@v20
        with:
          vercel-token: ${{ secrets.phKfgtkC6f3rJYjVODgJQ8IB }}
          vercel-org-id: ${{ secrets.pLFKeD5jmZQRNRErDc5suoU0 }}
          vercel-project-id: ${{ secrets.prj_RK8GoBiKH3UkyWR9dBOkt8ody2yJ }}